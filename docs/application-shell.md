# Application shell

The Gradio shell provides System, Knowledge Base, Documents, Chat, and Settings tabs. It never parses documents, embeds text, queries vectors, tokenizes values, calls LLMs, or reads knowledge-base internals directly.
